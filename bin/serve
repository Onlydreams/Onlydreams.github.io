#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
# shellcheck source=bin/preflight
source "$ROOT_DIR/bin/preflight"

run_project_bundle exec jekyll serve "$@"
